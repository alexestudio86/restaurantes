---

import {sections, posts} from "@/data/pGeneralInfo.json";
import planImages from "src/lib/importPImages.js";
const mapImage = planImages[sections.contact.map] ? planImages[sections.contact.map].src : planImages[posts.emptyImage].src

---

<aside class='w3-row' id="contacto">
    <div class="w3-row w3-auto w3-padding-64">
        <h2 class="w3-center w3-xxlarge w3-padding-large">Contacto</h2>
        <div class="w3-grid" style="grid-template-columns:1fr 1fr;">
            <div class="w3-flex" style="align-items:center">
                <p>soy la sección de contacto</p>
            </div>
            <div>
                <img class="w3-image" src={mapImage} alt={sections.contact.map} width="640" height="480px" />
            </div>
        </div>
    </div>
</aside>